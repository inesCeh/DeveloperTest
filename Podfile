# Uncomment the next line to define a global platform for your project
source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '13.0'
use_frameworks!

target 'DeveloperTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DeveloperTest
	pod 'Kingfisher', '~> 5.0'
	pod 'Alamofire', '~> 5.2'

  target 'DeveloperTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DeveloperTestUITests' do
    # Pods for testing
  end

end
